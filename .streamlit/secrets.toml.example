# Copie ce fichier en "secrets.toml" (sans ".example") dans ce même dossier,
# puis remplace la valeur ci-dessous par ta vraie clé Anthropic
# (console.anthropic.com -> API Keys). Ce fichier .example est committé au
# repo comme documentation ; "secrets.toml" (le vrai) est exclu par
# .gitignore et ne doit JAMAIS être committé.

ANTHROPIC_API_KEY = "sk-ant-remplace-moi-par-ta-vraie-cle"
